import React, { Fragment } from 'react';
import { COMPANY_ID } from '@/settings';

export default () => (
  <Fragment>
    <div>
      <p
        style={{
          textAlign: 'center',
        }}
      >
        欢迎使用。
      </p>
    </div>
  </Fragment>
);
